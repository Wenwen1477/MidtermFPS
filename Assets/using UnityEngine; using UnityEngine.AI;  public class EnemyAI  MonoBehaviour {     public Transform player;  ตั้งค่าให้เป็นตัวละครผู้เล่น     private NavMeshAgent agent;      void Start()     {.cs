using UnityEngine;

public class usingUnityEngine;usingUnityEngine.AI;publicclassEnemyAIMonoBehaviour{publicTransformplayer;ตั้งค่าให้เป็นตัวละครผู้เล่นprivateNavMeshAgentagent;voidStart(){ : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
